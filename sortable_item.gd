extends Button
#Generic class for items in sortable tables
class_name SortableItem

#The variables displayed in the row item, and their values
#Use a specialized subclass to load the correct variables
#These should be VariableHolders, a node that can easily be reused to display the variables
var variables = {
	
}

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
